export interface CommonComplaintDto {
  // userId: string | null;
  // photoUrls?: string[];
  name?: string;
  categoryId: string;
  countryCode: string;
  cityName?: string;
  shortDescription: string;
  fullDescription: string;
}
