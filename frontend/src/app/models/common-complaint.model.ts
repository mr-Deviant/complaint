export interface CommonComplaintModel {
  // userId: string | null;
  // photoUrls?: string[];
  name: string;
  categoryId: string;
  countryId: string;
  cityName?: string;
  shortDescription: string;
  fullDescription: string;
}
