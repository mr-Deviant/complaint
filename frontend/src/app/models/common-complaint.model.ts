export interface CommonComplaintModel {
  // userId: string | null;
  // photoUrls?: string[];
  name: string;
  categoryId: string;
  countryCode: string;
  cityName?: string;
  shortDescription: string;
  fullDescription: string;
}
